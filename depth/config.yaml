Directory:
  exp_name: test
  data_dir: ./depth/data/
  log_dir: ./tensorboard
  weights: ./weights
  results: ./results

Train:
  use_shuffle: True
  train_log_interval: 50
  valid_log_interval: 50
  train_plot_interval: 200
  valid_plot_interval: 200
  img_w: 640
  img_h: 480
  weight_decay: 0.00001
  batch_size: 16 # 1
  epoch: 101
  init_lr: 0.001